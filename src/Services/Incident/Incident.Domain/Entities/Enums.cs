namespace Incident.Domain.Entities;

public enum Objectenum {
    Pending,
    In_Process,
    Resolved,
    Canceled,
}

public enum Complainenum
{
    Pending,
    In_Process,
    Resolved,
    Dismissed,
}

public enum Tickets
{
    Claim,
    Found,
    Match,
    Pickup
}
